version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - VERSION=wedding&& npm run build
  artifacts:
    baseDirectory: dist   # or dist, depending on your framework
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*